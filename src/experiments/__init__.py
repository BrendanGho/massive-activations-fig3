"""Experiments built on top of the Figure-3 numeric core (channel ranking, clustering,
capture). Each experiment is a self-contained driver that reuses ``src/common`` and the
stage functions without modifying the fig3 pipeline.
"""
